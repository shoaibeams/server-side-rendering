import React from 'react'

const HomePage = () => {
  return (
    <div>
      Home Page
      <button onClick={() => console.log('object')}>Press Me!</button>
    </div>
  )
}

export default {
  component: HomePage
}
